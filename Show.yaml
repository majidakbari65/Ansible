- name: ping module demo
  hosts: all
  become: false
  tasks:
    - name: test connection
      ansible.builtin.ping:
    
    - name: Pinging Host
      action: ping
      
    - name: Execute ping
      shell: "ping -c 4 192.168.10.101"
      register: result
      
    - name: Show result
      debug:
        msg:"{{ result['stdout_lines'] }}"
